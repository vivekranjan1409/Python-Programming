# Sets is a collection of non repetitive elements
# Sets are unordered
# sets are unindexed
# there is no way to change items  in a sets
# Sets cannot contain duplicate values

# a = {}   This syntax will create an empty dectionary and not an empty set

# An empty set can be created using the below syntax:
b = set()
print(type(b))

a = {1,2,3,4,5}
print(type(a))
print(a)